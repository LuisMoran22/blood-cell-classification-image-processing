# Data

Place the dataset locally in this folder.

Expected structure:

```text
data/
└── dataset2-master/
    └── dataset2-master/
        └── images/
            ├── TRAIN/
            └── TEST/
```

The full dataset is intentionally ignored by `.gitignore` and should not be uploaded to GitHub.
