# Data Folder

Place the dataset locally here:

```text
data/
└── dataset2-master/
    └── images/
        ├── TRAIN/
        ├── TEST/
        └── TEST_SIMPLE/
```

The full dataset is ignored by `.gitignore`.
