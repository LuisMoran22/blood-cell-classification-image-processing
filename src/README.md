# Source Code

The uploaded archive did not include the original implementation code.

Add the project scripts here, for example:

```text
src/
├── preprocessing.py
├── segmentation.py
├── classification.py
├── evaluation.py
└── main.py
```

Once the code is added, update the main `README.md` with installation and execution instructions.
